# Flink配置
flink.checkpoint.interval=5000
flink.statebackend.test=false
flink.statebackend.path=

# Kafka配置
kafka.bootstrap.servers=localhost:9092
kafka.group.id=test
kafka.auto.offset.reset=earliest
kafka.enable.auto.commit=false
kafka.topics=flink-test

# Redis配置
redis.host=127.0.0.1
redis.port=6379
redis.database=3
redis.password=